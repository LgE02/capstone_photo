# ── 이미지 생성 (FLUX.2-klein-4B) ───────────────────────────
torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.25.0
transformers>=4.35.0
accelerate>=0.24.0
safetensors>=0.4.0
huggingface_hub>=0.19.0
Pillow>=10.0.0
numpy>=1.24.0
tqdm>=4.65.0

# ── OpenAI Chat Completion (스토리 분석, 기본 gpt-5.5) ──────
openai>=1.30.0
python-dotenv>=1.0.0

# ── 카프카 컨슈머 (입력) + producer (결과 publish) ──────────
aiokafka>=0.10.0

# ── AWS S3 (이미지 저장) ───────────────────────────────────
boto3>=1.34.0

# ── 선택: torchao FP8 양자화 (VRAM 절감) ────────────────────
# pip install torchao

# ── 선택: bitsandbytes (8-bit 옵티마이저) ──────────────────
# pip install bitsandbytes
